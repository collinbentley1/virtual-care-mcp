name: Infrastructure

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize

permissions: {}

jobs:
  infrastructure:
    permissions:
      contents: read
      id-token: write # Permission ceiling for the main-only convergence job in the trusted reusable workflow.
    uses: collinbentley1/platform/.github/workflows/infrastructure.yml@d0d1a3af340acb712caa5a1c83283a0387c87571
