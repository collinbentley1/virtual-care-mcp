name: Infrastructure

on:
  pull_request:
    types:
      - edited
      - opened
      - reopened
      - synchronize

permissions: {}

jobs:
  infrastructure:
    permissions:
      contents: read
      id-token: write # Permission ceiling for the main-only convergence job in the trusted reusable workflow.
    uses: collinbentley1/platform/.github/workflows/infrastructure.yml@d4c1bcf6e5700d2a4f75d96b9e09eb122e4d1672
